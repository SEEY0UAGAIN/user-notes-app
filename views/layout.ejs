<!DOCTYPE html>
<html>
  <head>
    <title>User Notes App</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Bootstrap Icons -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      /* ทำให้ main มีความสูงเต็มหน้าจอ */
      main.fullscreen-center {
        min-height: 80vh; /* ปรับได้ตามต้องการ */
        display: flex;
        justify-content: center;
        align-items: center;
      }

      /* Card hover effect */
      .note-card:hover {
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
        transform: translateY(-4px);
        transition: all 0.3s ease;
      }

      /* ฟอร์ม add note แบบ transition */
      #noteForm {
        transition: max-height 0.4s ease, opacity 0.4s ease;
        overflow: hidden;
      }
      #noteForm.hidden {
        max-height: 0;
        opacity: 0;
        pointer-events: none;
      }
      #noteForm.visible {
        max-height: 500px; /* สูงสุดของฟอร์ม */
        opacity: 1;
        pointer-events: auto;
      }
    </style>
  </head>
  <body>
    <header
      class="bg-primary text-white p-3 mb-4 d-flex justify-content-between align-items-center shadow-sm"
    >
      <h1 class="h3 m-0">User Notes App <i class="bi bi-journal-text"></i></h1>
      <% if (typeof user !== "undefined") { %>
      <a href="/logout" class="btn btn-light btn-sm">
        <i class="bi bi-box-arrow-right"></i> Logout
      </a>
      <% } %>
    </header>
    <main
      class="<%= typeof mainClass !== 'undefined' ? mainClass : '' %> container"
    >
      <%- body %>
    </main>

    <!-- Bootstrap JS Bundle (Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
